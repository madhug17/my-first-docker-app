global:
  scrape_interval: 5s

scrape_configs:
  # Prometheus itself
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # FastAPI app
  - job_name: "fastapi-app"
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:8000"]